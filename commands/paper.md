# /paper

Use `paper-core-writing` for paper core, `research-method-writing` for a code-aligned Method section, and `evidence-based-writing` for other sections.

Required output: pitch, abstract, contributions, outline, evidence map, writing risks, and a method-to-code map when implementation is available.
