# /code-idea

Use `github-repository-operations` when acquisition is needed, then `code-to-research-idea`.

Default private clone: `gh repo clone OWNER/REPO [DEST] -- --depth=1`.

Required output: code evidence map, observed/inferred/unverified separation, candidate ideas, search anchors, novelty risks, and next literature action.
