# /github

Use `github-repository-operations`.

Default clones to `--depth=1`. Require explicit public/private visibility for creation. Before push, inspect status and diff, exclude sensitive or large artifacts, and verify the remote branch.
